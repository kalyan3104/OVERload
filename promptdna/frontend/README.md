# PromptDNA Frontend

Simple React admin UI to submit prompts to the PromptDNA middleware and inspect recent logs.

## Local Development

```bash
npm install
npm start
```

The app expects the backend running at `http://localhost:8000`.
